
# Wrapper Directory

This directory contains the high-level API logic that handles user requests and interacts with the models.

## Files

- **`runner.py`**: 
    - Contains the core `spliceAI` function.
    - Handles model loading, input preprocessing, prediction loop, and result formatting.
    - Manages device allocation and batch processing (if extended).
